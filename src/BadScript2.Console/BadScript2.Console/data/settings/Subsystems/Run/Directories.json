{
  "Subsystems": {
    "Run": {
      "RootDirectory": "$(Console.SubsystemDirectory)/run",
      "LibraryDirectory": "$(RootDirectory)/libs",
      "StartupDirectory": "$(RootDirectory)/startup",
      "AppDirectory": "$(RootDirectory)/apps",
      "AppDataDirectory": "$(RootDirectory)/appdata"
    }
  }
}