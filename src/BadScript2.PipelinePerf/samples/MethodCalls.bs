// Benchmark: Method Calls (Methodenaufrufe)
// Tests the performance of function calls

function Add(a, b) => a + b;

function Calculate() {
    let result = 0;
    for(let i = 0; i < 100000; i = i + 1) {
        result = Add(result, 1);
    }
    return result;
}

Calculate();

