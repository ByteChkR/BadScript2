function F(x, y)
{
        //Do some stuff that takes a while

        return x + y;
}

let value = await F.AsTask("Hello ", "World"); //Runs F as Task with the arguments provided.

Console.WriteLine(value);