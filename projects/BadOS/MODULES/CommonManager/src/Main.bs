
function BuildCommonApi(ctx, table)
{
	table["Json"] = Json;
	table["Compression"] = Compression;
	table["Native"] = Runtime.Native;
	table["EvaluateAsync"] = Runtime.EvaluateAsync;
	table["CreateDefaultScope"] = Runtime.CreateDefaultScope;
}

ApiBuilder.Add(BuildCommonApi);
